import { ExperienceLabelPipe } from './experience-label.pipe';

describe('ExperienceLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new ExperienceLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
